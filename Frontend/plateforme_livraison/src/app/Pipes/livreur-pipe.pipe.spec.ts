import { LivreurPipePipe } from './livreur-pipe.pipe';

describe('LivreurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LivreurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
